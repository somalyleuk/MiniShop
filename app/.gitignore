# Android / Gradle build outputs
build/
.gradle/

# IDE settings
.idea/
*.iml

# Kotlin compilation cache
.kotlin/
.kotlin

# Proguard rules (Keep local, but usually optional to ignore unless auto-generated)
proguard-rules.pro